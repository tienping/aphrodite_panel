// import { fromJS } from 'immutable';
// import { selectMallDomain } from '../selectors';

describe('selectMallDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(true);
    });
});
